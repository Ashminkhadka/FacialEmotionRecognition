import React from 'react';

const RadarGraph = ({ data }) => {
  // Add code to display radar graph using 'data'
  return (
    <div>
      {/* Radar graph component */}
    </div>
  );
};

export default RadarGraph;
